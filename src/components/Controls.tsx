import * as React from 'react';

export function Controls() {
  return (
    <span className="controls">
      Use [A][S][W][D] or [←][↑][↓][→] to MOVE
      <br />
      Use [SPACE] to SHOOT
    </span>
  );
}
